asyncapi 2.3.0
info
  title My API
  version '1.0.0'
channels
  mychannel
    publish
      message
        schemaFormat applicationvnd.apache.avro;version=1.9.0
        payload
          $ref 'schemasissue-113-testcase.avsc'